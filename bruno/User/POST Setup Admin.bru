meta {
  name: POST Setup Admin
  type: http
  seq: 2
}

post {
  url: {{base_url}}api/auth/admin
  body: json
  auth: inherit
}

body:json {
  {
    "password": "teste123"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
